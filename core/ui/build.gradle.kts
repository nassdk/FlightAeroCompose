plugins {    id("com.android.library")    kotlin(module = "android")}android {    compileSdk = 31    defaultConfig {        minSdk = 23        targetSdk = 31        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"        consumerProguardFiles("consumer-rules.pro")    }    compileOptions {        sourceCompatibility = JavaVersion.VERSION_1_8        targetCompatibility = JavaVersion.VERSION_1_8    }    composeOptions {        kotlinCompilerExtensionVersion = "1.0.5"    }    packagingOptions {        resources.excludes.add("/META-INF/{AL2.0,LGPL2.1}")    }    buildFeatures {        compose = true    }    kotlinOptions {        jvmTarget = "1.8"    }}dependencies {    libs.run {        api(dependencyNotation = bundles.composeUi)        api(dependencyNotation = material)        api(dependencyNotation = androidx.corektx)        api(dependencyNotation = androidx.appCompat)    }}